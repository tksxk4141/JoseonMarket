using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class cshSkyLight : MonoBehaviour
{
    GameObject[] paper;
    bool isfold;
    
    // Start is called before the first frame update
    void Start()
    {
        isfold = false;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
